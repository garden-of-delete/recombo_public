#include "mmchain.h";

class librarian{
private:
	ifstream in;
	ofstream out;
	string filename;
	int n_components, min_arc, max_arc;
public:
	librarian(string filename): n_components(0), min_arc(0), max_arc(0) {};

};
